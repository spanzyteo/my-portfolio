import { Box, Stack, Typography } from '@mui/material'
import backgroundImage from '../images/transparent-bg-portfolio.png'

const Projects = () => {
  return (
    <Box sx={{ backgroundImage: `url(${backgroundImage})` }}>Projeccts</Box>
  )
}

export default Projects
